#!/bin/sh

update()
{
  sudo apt update
}

upgrade()
{
  sudo apt upgrade -y
}

autoclean()
{
  sudo apt autoclean -y
}

dist-upgrade()
{
  sudo apt dist-upgrade -y
}

do-release-upgrade()
{
  sudo do-release-upgrade
}

update && upgrade && dist-upgrade &&  autoclean && do-release-upgrade
